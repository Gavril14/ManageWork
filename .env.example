# ═══ FRONTEND ENVIRONMENT VARIABLES ═══

# Backend API URL (leave empty for dev mode — runs without backend)
# When backend is running, set this to connect:
# VITE_API_URL=http://localhost:4000/api/v1

# For production:
# VITE_API_URL=https://api.your-domain.co.uk/api/v1
